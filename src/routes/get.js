const createcollege= async function {

    let data = req.body
    if(!data){
        res.status(400).send({})
    }

if(Object)

const {name,fullname,logo}=data

if(!name){
    res.status(400).send({})
}

}